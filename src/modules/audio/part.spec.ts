import { expect } from 'chai';
import Part from './part';

describe('part', () => {
  it('works', () => {
    expect(5).to.equal(5);
  });
  it('works', () => {
    // const part = new Part();
    // const ctx = new AudioContext();
  });
});
