import store from '@/store/store';

export { default as cache, Cache } from '@/store/cache';
export { default as workspace, Specific } from '@/store/workspace';
export { default as general, General } from '@/store/general';
export { Project } from '@/store/project';
export default store;
